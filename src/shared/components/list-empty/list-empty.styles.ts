import { StyleSheet } from 'react-native';

import { COLORS } from '../../styles';

export const styles = StyleSheet.create({
	text: {
		fontSize: 16,
		lineHeight: 18,
		color: COLORS.black,
		textAlign: 'center',
	},
});
