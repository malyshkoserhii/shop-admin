import { css } from '@emotion/css';

import { FONT_FAMILIES } from './fonts';
import { COLORS } from './colors';

export const fontStyleRegular = css`
	font-family: ${FONT_FAMILIES.poppins_regular};
	font-size: 14px;
	color: ${COLORS.black};
`;
